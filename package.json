{
  "name": "repayments",
  "version": "1.0.0",
  "description": "",
  "main": "repayment.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel --watch src --out-dir build --presets react-app/prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.0.0"
  }
}
